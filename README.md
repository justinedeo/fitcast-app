# fitcast-app
CSCI 440

# to start make sure docker desktop is running
# The put cd start-my-docker in the terminal
# Then type ls to see what there is
# to build the container: docker build -t fitcast-app .
# to run it: docker run fitcast-app
# if you want to start it by ID. type this into the terminal: docker start <container_id>
# Example: docker start 10c8ca5ae3b2
# to see all the information: docker ps -a
